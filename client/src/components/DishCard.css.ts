import * as styles from './DishCard';
import { style } from '@vanilla-extract/css';
import { vars } from '../styles/themes.css';

export const flex = style({
  flexDirection: "row",
  maxWidth: "100%",
})